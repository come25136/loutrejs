---
'@loutrejs/loutre': minor
'create-loutre': minor
---

Add `AsyncDisposable` support to Application handles so applications can be scoped with `await using`, and generate projects with the matching disposable TypeScript library support.
