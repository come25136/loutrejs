services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: loutre
      POSTGRES_PASSWORD: loutre
      POSTGRES_DB: loutre_prisma
    ports:
      - "54323:5432"
    volumes:
      - ./schema.sql:/docker-entrypoint-initdb.d/schema.sql:ro
